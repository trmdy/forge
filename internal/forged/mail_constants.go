package forged

const (
	// DefaultMailPort is the default TCP port for the Forge Mail server.
	DefaultMailPort = 7463
	// defaultMailSocketName is the socket filename under .fmail/.
	defaultMailSocketName = "forged.sock"
)
